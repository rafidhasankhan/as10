import React from 'react';
import './NotFound.css'

const NotFound = () => {
    return (
        <div className="not_found">
            <h1>Whoops! This is not what you were looking for</h1>
            <h5>But you just found the sock we'd lost, thanks. Try your luck by going back to the home page.</h5>
        </div>
    );
};

export default NotFound;